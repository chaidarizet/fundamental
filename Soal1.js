var number = [1,5,4,-5];
var cek = Math.sign('-',number)

    var cek = number < 0;
    for(i=0;i<number.length;i++)
    {
        
        if (number <=0)
        {
            hitung = number + number
            console.log('Minus Win')
        }
        else if(number>=0){
            hitung = number[i] + number[i];
            console.log('Plus Win')
        }
        else if(number == number)
        {
            console.log('Plus and Wind is Draw')
        }
    }

